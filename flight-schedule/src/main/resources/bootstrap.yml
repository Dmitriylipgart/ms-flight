spring:
  profiles:
    active: dev
  cloud:
    config:
      name: flightSchedule
      uri: http://localhost:8081
eureka:
  client:
    healthcheck:
      enabled: true
encrypt:
  key: flight-ms-secret